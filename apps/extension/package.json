{
  "name": "@telex/extension",
  "version": "0.0.1",
  "private": true,
  "description": "telex browser extension",
  "type": "module",
  "scripts": {
    "build": "wxt build && wxt zip",
    "dev": "wxt dev",
    "postinstall": "wxt prepare",
    "zip": "wxt zip"
  },
  "dependencies": {
    "@telex/engine": "workspace:*"
  }
}
