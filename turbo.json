{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "test": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".output/**", ".wxt/**", ".next/**", "dist/**"]
    },
    "check": {
      "dependsOn": ["build"],
      "cache": false
    },
    "fix": {
      "dependsOn": ["build"],
      "cache": false
    }
  }
}
